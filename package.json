{
  "name": "@nk-ui/core",
  "version": "1.0.1",
  "description": "UI Components for consistent design",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish:beta": "npm publish --tag=next --access public",
    "build": "npx tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
